# openshelf — 合法全文聚合器

**交接自** `ses_02ad5cab8ffeg5C55RAMQIk4Ey`（opencode `[★]main`），2026-08-19。
題目在 opencode session 起頭，但與該 repo 無關，故獨立成案。

---

## 使用者要什麼（他親自選的，不是我推測）

問：「聽說過 libgen 嗎？我需要開發一個替代系統」

四選一 → **合法全文聚合器**（把已合法開放的來源聚成好用的檢索+取得介面）
規模 → **只給自己 / 小圈圈**（NAS 或單台 VPS，不對外開放）
藏書 → **合一**：自己的 PDF/EPUB 與公開來源同一個搜尋框
用途 → **五個全選**：找特定一篇拿全文 / 主題探索 / 長期保存 / 餵 RAG / 閱讀

### 已對使用者言明的邊界

libgen 的核心是未授權散布受版權內容，**不做**。難點幾乎全在躲查緝（域名輪替、
抗查封鏡像、匿名金流），做了等於承擔法律風險且不能掛名。

**已明確告知使用者：現代書 / 教科書這一格，合法聚合器覆蓋率趨近於零，沒有技術解。**
他知道這件事之後仍選這個方向 —— 不要再重複這個提醒，也不要為了「補齊」而滑向灰色地帶。

---

## 五個用途收斂到同一個地基

```
全文真的落到本地，而且被解析過
```

找特定一篇 = 查索引拿檔案 / 主題探索 = 全文搜尋 / 長期保存 = 檔案在硬碟上
閱讀 = 直接開 / RAG = 拿解析文字切塊。**一個地基餵五個用途。**

唯一衝突：閱讀要原始 PDF 排版，RAG 要乾淨純文字。**兩份都留**
（解析文字約為 PDF 的 3–5%，個人規模下不是成本問題）。只留一份才是錯的。

---

## 架構形狀

```
取得層     OpenAlex dump（metadata 主幹）+ 各來源 bulk dump + 自己的 PDF/EPUB
              ↓
正規化層   統一文件模型 ← 「合一」的代價集中在這層
           去重 + 版本判定 ← 全案最貴的一格
              ↓
落地層     原檔 + 解析文字 + metadata，三份都留
              ↓
索引層     關鍵字全文索引（找/探索）+ 向量索引（RAG/相似）
              ↓
介面層     搜尋 / 閱讀器 / RAG API
```

**文件模型定錯，後面每一層都要改。** 使用者的書沒有 DOI、沒有引用數；
OpenAlex 的紀錄沒有本地路徑、沒有閱讀進度。同一個搜尋框排序要能容納兩者。

---

## 分期（每期都要能單獨用，不要做半套才驗收）

```
第一期  自己的藏書 + 全文搜尋 + 閱讀器
        ← 先做這個不是因為簡單，是為了在投入數百 GB 之前先驗證解析品質與文件模型
第二期  接 OpenAlex metadata + 全文定位   ← 難點全在去重
第三期  全量 dump 落地 + 增量同步        ← 真正做到「不會消失」
第四期  RAG 層                           ← 前三期做對，這期只是接上去
```
